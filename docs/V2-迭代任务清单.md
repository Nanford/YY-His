# V2 迭代任务清单（断点续作入口）

> **用法**：本文件是 V2 迭代的唯一进度事实来源。每完成一项就把 `- [ ]` 改为 `- [x]` 并注明完成日期/关键产物；
> 中途停止时在「当前断点」写清"做完什么、下一步从哪开始、有什么遗留问题"。
> **新会话恢复方法**：读本文件 → 找第一个 `- [ ]` → 读「当前断点」→ 继续。
> 计划全文见会话计划文件（V2 迭代开发计划），需求事实来源：V2/ 文件夹（4 张 xlsx + docx + figure 1.png，只读）。

## 待拍板（未明确前不编造规则）

- [ ] 执行节奏确认：默认 MVP 优先分阶段（M6→M7→M8 先行演示，再 M9–M11）
- [ ] 规则歧义与甲方确认：NRS2002 初筛4 缺题干/字段/判定定义；MMSE 无法完成标签缺触发路径；便秘“已诊断/未诊断”缺判定路径；疼痛行为量表 Q3 呼吸机条目原样保留。未拍板项不自动产出答案或标签，交由医护补录或明确标记待定义。

## 关键口径（已锁定，实现与测试按此）

- 推荐唯一事实来源 = 03 表矩阵：100 强制置顶、-100 禁止剔除、0 忽略、2–10 累加；每大类取前 2，5 大类最多 10 项；5 个 `{score}` 占位标签不进推荐；V1"倾向是/基本是视同正式标签"口径**废止**
- 中医体质：30 题新版，转化分 ≥40 是 / 30–39 倾向是 / <30 否；平和质 ≥60 且其他 8 质 <30 = 是、<40 = 基本是；湿热质性别互斥题按"不适用"剔出分母
- MNA-SF 改两档：12–14 未提示风险 / ≤11 营养不良风险
- 矩阵按**标签编码**索引（不用中文名）；干预编码 YD/SS/ZY/JZ/QT 为素材关联唯一标识
- 100 分全表仅 1 条：CAM_DELIRIUM_POSITIVE → JZ01；-100 共 37 条：谵妄禁 YD/SS/ZY 全 30 项 + YD07 另禁 7 标签
- 复用规则：同变量编码跨量表只问一次，阴性回填按 01 表复用规则列
- docx 明示暂不接入：握力计传感器、DXA/BIA、自动动作测试（SPPB/耳语/洼田/Braden/CAM 等走医生端代填）

---

## M6 数据管线重写（纯转换，不改运行时）

- [x] M6.1 设计 V2 数据 schema（scales-v2 / result-tags / intervention-scoring / interventions-v2 四个 JSON 的字段契约，写进 `data/README.md`）
- [x] M6.2 convert-rules 解析 01_评估采集规则表 → `data/scales-v2.json`：42 量表、238 条目 + 20 旁白（题号/题干/选项分值/条目类型/变量编码/复用规则/开场白过渡句），生成期校验行覆盖与选项分值合法（注：原计划估计"38 量表/261 行"，以 xlsx 实际 42 量表/258 数据行为准）
- [x] M6.3 convert-rules 解析 02_结果标签判定表 → `data/result-tags.json`：190 标签（编码/中文名/所属量表/判定规则结构化），与 01 表量表名交叉核对
- [x] M6.4 convert-rules 解析 03_标签干预匹配表 → `data/intervention-scoring.json` 重写：编码索引矩阵 190×60，生成期校验 11400 条完整笛卡尔积 + 无重复对 + 分值落在 {-100,0,2–10,100}
- [x] M6.5 convert-rules 解析 04_干预方案信息表 → `data/interventions-v2.json`：60 项（编码/名称/正文/展示形式/大类/素材 URL+内容哈希），与 03 表干预编码互相核对
- [x] M6.6 figure 1.png palette 压缩 → `public/interventions/bristol-stool.png` + 派生 webp（复用现有压缩工具函数）
- [x] M6.7 转换测试：黄金向量单测（FRAIL_FRAIL、SARCOPENIA_DIAGNOSED、CAM_DELIRIUM_POSITIVE→JZ01:100、MORSE_FALL_RISK_HIGH→YD07:-100 等 10 条已提取向量）+ 190 标签/60 干预全集核对测试
- [x] M6.8 V1 旧数据文件（tag-mapping.json / interventions.json / 旧 M·D·C 素材）标记退役不删，`data/README.md` 说明

## M7 评分引擎泛化 + 常规综合评估包 8 量表

- [x] M7.1 judgment 类型扩展：sumRange（N/A 剔除语义仅 tcmConstitutionV2 使用）、anyYes、tcmConstitutionV2（转化分）已落地；**perQuestionTags/thresholdByEducation/composite 推迟到 M10 随对应量表（居家环境/MMSE/GLIM/CAM）落地**（2026-08-06）
- [x] M7.2 通用评分器按 judgment.type 派发 + 注册表数据驱动——**实际落地路径为 `src/lib/scoring-v2/`（types/common/sum-range/any-yes/tcm-constitution/index），不是原计划的 src/lib/scoring/generic.ts**；规则读取入口 `src/lib/rules/v2.ts`；判定配置 `data/judgments-v2.json`（手工整理 + convert-rules-v2 校验段）
- [x] M7.3 新 answerType：number 引擎层已支持（合法分值校验 + 用例锁定），multiChoice/imageChoice 引擎层语义预留（选项 label 匹配机制兼容），UI 在 M9
- [x] M7.4 落地 8 量表：ADL、IADL、FRAIL、MNA-SF（两档判定）、Mini-Cog（Q2 画钟按医生确认计分、Q1 记忆指令不计分）、抑郁两问、焦虑两问、中医体质 30 题转化分版（27 计分题 + 湿热质性别互斥 N/A 剔分母）
- [x] M7.5 测试：`tests/scoring-v2-{sum-range,any-yes,tcm,common}.test.ts` 共 80 用例全绿——每量表全量边界、转化分边界（41.7/33.3/25、62.5/56.3）、deferClinical 沿用、湿热质性别互斥 N/A、tagCode↔190 标签核对；MMSE 文化分层随 M10 落地

## M8 推荐引擎 V2 + 干预展示改版

- [x] M8.1 推荐引擎 V2（落地为**新模块** `src/lib/recommend-v2/`，不重写旧目录：编码索引、100 强制置顶 / -100 一票禁止 / 2–10 累加、5 大类各取前 2（forced 置顶不占名额）、占位标签过滤、积分明细下钻 + forbidden 禁止原因明细）。**运行时装机（dialogue/finalize/报告页改接 V2 数据）在 M9 统一做**，旧 `src/lib/recommend/` 保持不动
- [x] M8.2 `intervention-media.tsx` 新增文本卡片 `InterventionText`（JZ/QT 文本项，纯增量导出、未改现有组件）；`CATEGORY_ORDER` 改 5 类**推迟 M9 装机**（旧报告页仍跑旧 3 类数据，改它会破坏现有渲染）
- [x] M8.3 报告页"先评估结论、后干预建议"——**核查结论：现状已满足，两端均未改代码**（患者端 `patient-report.tsx` TagsSection 在 PlanSection 前；医生端 `page.tsx` ResultView 在 PlanReview/FinalPlan 前）；JZ01 强制置首并醒目标注随 M9 装机接入（引擎已输出 `forced` 平铺列表供其使用）
- [x] M8.4 60 项正文已入库（M6.5 `data/interventions-v2.json` 的 `content`）；素材缺失标"素材待补齐"的展示逻辑（`mediaAvailable=false`）随 M9 装机接入（AI 生成素材不在本里程碑）
- [x] M8.5 测试：`tests/recommend-v2.test.ts` 13 用例全绿——黄金用例（5 标签 × 5 类前 2 与总分，期望值手工抄自矩阵硬编码）+ CAM→JZ01 强制 + 谵妄禁 30 项 + YD07 禁忌（MORSE/ADL 重度）+ 每类前 2 截取 + 同分按编码升序 + placeholder 剔除 + 未知编码抛错 + forced 不占名额 + 积分明细顺序稳定

## M9 基础信息扩展 + 采集编排 + 新题型 UI

- [x] M9.1 Patient 模型扩展：文化程度、婚姻、居住、照护、诊断清单、用药清单、体重史（当前/1/2/3/6/12 月前）、握力、6 米步速、小腿围双侧 → prisma migrate（`20260806130939_v2_patient_intake`）+ 医生端完整表单/患者自助简版（2026-08-06）
- [x] M9-B.1 适配层投影（2026-08-06）：`src/lib/rules/index.ts` 从 V2 数据（`data/scales-v2.json` + `data/judgments-v2.json`）投影旧形状——9 个已配判定的可评分量表（adl/iadl/minicog/depression_2q/anxiety_2q/fall_3q/frail/mnasf/tcm_constitution，按 01 表文档顺序）；条目类型 ≠ 正式问题的计分条目标 `observerAssisted`（系统读取/逻辑计算/绘图操作等），患者端不问、缺失按 deferClinical 豁免
- [x] M9-B.2 finalize 接 V2（2026-08-06）：`src/lib/assessment/finalize.ts` 评分走 `src/lib/scoring-v2`、推荐走 `src/lib/recommend-v2`，快照/落库形状适配
- [x] M9-B.3 页面与动作适配（2026-08-06）：`src/lib/actions/doctor.ts` / `collect-form.tsx` / `plan-review.tsx` / `patient-report.tsx` / 患者详情与新建页 / `patient/register/page.tsx`（自助默认勾选改 frail+fall_3q）
- [x] M9-B.4 旧引擎与旧测试删除（2026-08-06）：`src/lib/scoring/*`、`src/lib/recommend/`、`src/lib/assessment/measurement*.ts` 及对应旧测试（scoring-*/recommend/measurement-answers）移除
- [x] M9-B.5 测试改写（2026-08-07）：`tests/dialogue-state-machine.test.ts`（黄金路径改 frail+fall_3q 共 6 题；跳过用例改断 frail_4/frail_5 系统读取 + minicog_2 绘图操作等 observerAssisted 条目）、`tests/dialogue-normalize.test.ts`（fixture 换 frail_1/fall_3q_1/mnasf_1/tcm_constitution_A.1-1，布尔 label 带括注断言）、`tests/patient-intake.test.ts`（9 量表 01 表顺序、默认 fall_3q+frail 归一化、全选 9 量表）、`tests/supplementary.test.ts`（scaleNeedsClinician 新口径：frail/mnasf/minicog=true，tcm_constitution 等=false）；顺带修复 `normalize-rules.ts` parseBoolean 对 V2 带括注布尔 label（"是（1分）"等）查找不到的手误级 bug（按括号前主文案匹配）；全量 190 用例全绿、typecheck/lint/build 通过
- [x] M9.2 采集编排引擎：总开场/分类过渡/工具说明按 01 表顺序播报（2026-08-07）：状态机 `buildTimeline(scaleIds)`（总开场恒最前；分类过渡 scaleId 为空时按 01 表行号锚定"其后第一个量表"；只纳入勾选量表锚定的旁白、插在该量表首题之前、同量表多条按行号排序；空正文旁白行如 112 行跳过）+ `nextStep` 改消费 timeline（新增 `{kind:"narration"}` 步骤与 snapshot `deliveredNarrationIds`，从 role=system 轮次派生，刷新重放不重复播）；service 新增 `advancePatientNarration`（旁白 system 轮次唯一写入点，播完才落库）+ DTO 新 phase `narration`/`PatientNarrationDto`；新路由 POST `/api/patient/sessions/[id]/advance`；interview-screen 旁白气泡（「说明」徽章、无作答区、播完自动 advance，「继续」按钮兜底刷新无手势场景）。取舍：保留 OPENING_TEXT 作"说开始"确认触发器（consent VAD 那套不动），01 表总开场 narr_3 在确认后作为第一条旁白播报，两者文案不重复（一个征求开始、一个内容总览）。测试：state-machine 测试改 timeline 适配 + 新增 5 用例（时间线顺序/只播勾选量表/空行跳过/播报后不重复/无答案走到 finished），全量 218 用例绿、typecheck/lint/build 通过
- [x] M9.3 变量编码复用注册表（2026-08-07）：`src/lib/assessment/reuse.ts` 纯函数 `deriveReuseAnswers(scaleIds, confirmed)` + 手工维护的 REUSE_RULES 注册表。实际落地范围：当前 13 个可评分量表内 01 表成立的复用仅**焦虑两问 ↔ GAD-7** 一组（共享变量编码 ANXIETY_TENSION_2W/ANXIETY_UNCONTROLLABLE_WORRY_2W；源题答「否（筛查阴性）」→ gad7_1/gad7_2 回填 0 分档，答「是」只追问频率不回填）；目标 label 从 scales-v2 按 0 分反查不硬编码（查不到抛错）；触发值校验为源题合法选项。**抑郁两问→GDS-15 不做门控**（01 表未明说，注册表注释注明）；其余复用（运动初筛→FRAIL2/3、跌倒史→Morse1、尿失禁→ICIQ 等）涉及 morse/motor_screen/nrs2002 等未配判定量表，随 M10.3b 落地后扩充注册表
- [x] M9.4 分支逻辑（2026-08-07）：与 M9.3 同一套「已确认答案 → 回填/跳过」机制（回填落库为 confirmed，状态机天然跳过，无独立分支引擎）。接线两处：①对话 service（`submitPatientAnswer` 答案确认后 + `beginPatientQuestions`/`advancePatientNarration` 推进时跑 `syncReuseAnswers`，回填 source=system/confirmed 并同步进快照，随即便跳过；复用条件不再成立时旧系统回填撤回为 superseded + editHistory 留痕 operator=system，题目恢复待问）；②`finalize.scoreAndSnapshot` 评分前兜底同步（医生 CollectForm 改焦虑两问答案后 gad7 回填同步纠正；strict 路径下回填到位的 gad7 题不再阻断）。共用 helper 抽到 `src/lib/assessment/system-answers.ts`（`upsertSystemAnswers` 供 system-read 与 reuse 共用，finalize 原内联循环抽取，行为不变）；`tests/reuse.test.ts` 12 用例（含状态机层"回填即跳过"走查）。运动初筛阳性→SPPB、NRS2002 初筛→终筛等分支依赖未配判定量表，随 M10.3b 落地
- [x] M9.5 系统读取题自动作答落库（2026-08-07）：`src/lib/assessment/system-read.ts`（纯函数 `resolveSystemReadAnswers(patient, scaleIds, {existing})`，经 rules/v2 原生 entryType=="系统读取" 遍历，本期实现 frail_4（diagnoses≥5 种）/frail_5（峰值体重降≥5%）/mnasf_2（m3 体重差四档，"不知道"档不自动产生）/mnasf_6（BMI 四档→缺身高体重回退双腿取细小腿围；选项 label 全从规则数据按分值+关键字反查不硬编码）；`finalize.ts` scoreAndSnapshot 评分前落库 Answer（source=system、status=confirmed，人工 confirmed 优先不覆盖，旧 system 值变更走 appendAnswerEditHistory 留痕 operator=system）；追溯界面来源徽章加"系统读取"（trace-view.tsx + page.tsx TRACE_SOURCES）；`tests/system-read.test.ts` 23 用例全绿、全量 213 用例全绿、typecheck/lint/build 通过。morse_2/nrs2002_终筛3（年龄/性别类）虽可推出但口径未逐条拍板，本期未做；mnasf_6 小腿围 <31cm 的 0 分档因转换解析把 E 档并入 D 档 label，借同分值选项承载分值（rawText 记真实依据，详见 system-read.ts 注释）
- [x] M9.6 新题型患者端 UI（2026-08-07）：`answerType` 扩 number/multiChoice/imageChoice/drawing；`answer-input.tsx` 数字步进/多选/Bristol 图/画钟画布；API 扩 multi/drawing（score 上限 10）；ICIQ Q3 合成 0～10 选项 + sumRange；便秘 Q3 低频率标签；Q4 多选 ` || ` 拼接；画钟交卷 pending 等医生确认
- [x] M9.7 医生端医护题代填界面（2026-08-07）：CollectForm 支持数字/多选/图片参照/画钟预览；`persistAnswersFromForm` 聚合同名多选与数字分值反查 label；pending 答案与 rawText 画作可回填
- [x] M9.8 测试（2026-08-07）：`tests/scoring-v2-iciq-m96.test.ts` + per-question-tags/iciq 便秘用例改写；全量 394 用例绿

## M10 量表工具选择 + 剩余量表

- [x] M10.1 量表选择界面（医生端，2026-08-07）：常规综合评估包（8 量表，默认）/ 4 系统预设套餐（衰弱与机能/认知/情志/中医特色）/ 临时自定义组合（一级分类分组树，只列已配判定的 13 个可评分量表）/ 随访对比复评（取最近一次 collected/confirmed 会话范围，服务端口径防篡改）。产物：`src/lib/assessment/scale-packages.ts`（SCALE_PACKAGES + parseSessionScaleSelection，模块加载断言套餐全部可评分）、`src/app/doctor/patients/[id]/session-create-form.tsx`（客户端单选互斥 + 自定义树展开）、`createSession` 改用统一解析、`tests/scale-packages.test.ts` 14 用例。**病历智能评估入口（docx §2 LLM 推荐量表）属 M10.2 未做**，界面仅占位禁用
- [x] M10.2 病历智能评估（2026-08-07）：`emr-scale-suggest.ts`（脱敏 + 规则兜底 + DeepSeek JSON 推荐）+ `POST /api/doctor/emr-suggest` + 发起评估表单 EMR 入口；`tests/emr-scale-suggest.test.ts`
- [x] M10.3 剩余量表工具评分器/题库全部入库——**M10.3a（2026-08-07）** 预设套餐 13 量表；**M10.3b 第一批（2026-08-07）** 13→25；**M10.3b-2（2026-08-07）25→42 全量**：①新判定类型 `ladderScore`（视力/听力阶梯）、`anyBelowThreshold`（耳语双侧词数）、`initialGateSumRange`（NRS2002 初筛门控+终筛）、`compositeAllAny`（CAM 1∧2∧(3∨4)、GLIM 表现型∧病因型+重度代理）；②既有类型落地 motor_screen/sppb/braden/visual_function/home_env/pain_behavior/polypharmacy/calf/grip/gait_speed/dxa_bia；③convert-rules-v2 为 cam_1/whisper/测量项合成可解析 options；④system-read 扩 nrs2002 初筛1/2/终筛3 + calf/grip/gait 档案换算；⑤register 适老化文案补 17 量表；⑥`tests/scoring-v2-m10-batch2.test.ts` 21 用例，全量 388 用例绿、typecheck/convert-rules-v2 绿。**取舍**：GLIM 重度以 glim_2/glim_3 为代理（选项粒度不足全文重度标准）；测量项用合成阴阳性选项（数值录入属 M9.6）；iciq Q3 数字/多选与 constipation_symptom Q3 仍待 M9.6
- [x] M10.4 患者端默认勾选与适老化短标题/介绍文案字典更新（`patient/register/page.tsx`，默认仍勾 FRAIL+跌倒三问；M10.3b-2 补 17 量表短标题）

## M11 打磨与交付

- [x] M11.1 e2e（2026-08-07）：5 个 spec 按 V2 装机现实重写，`npx playwright test` 7 用例全绿（3.6m）——doctor-flow（常规综合评估包 8 量表全量代填 → 16 标签 → 5 大类 9 项候选 → 替换/删除/确认留痕 + 系统读取自动作答 + YD07 禁止推荐明细 + strict 阻断缺失系统读取题 + 空候选确认归档）、patient-flow（自助问答 → 自动出报告：部分计分标注/5 大类分组/文本干预文字卡/初步方案→医生已确认切换）、patient-self-register（13 量表多选默认 fall_3q+frail + 医生审核关卡不变量）、supplementary（补充评估独立会话 + 历史报告互访）、patient-voice-auto（假麦克风语音全免动手，旁白播报纳入等待窗口）。黄金期望值由 `tmp/e2e-golden-v2.ts` 探针用 scoring-v2/recommend-v2 实算后硬编码；未改任何 src/ 页面代码（未发现真 bug）；无 skip 用例
- [x] M11.2 报告页 190 标签展示策略（2026-08-07）：患者报告 TagsSection 按量表分组 + 异常标签/量表组置顶高亮；历史对比入口既有不变
- [x] M11.3 `npm run pack-share` 便携包验证（2026-08-07）：`dist/his-demo-portable.zip` 约 151MB，含 V2 迁移 4 条，启动 bat 可用
- [x] M11.4 文档更新（2026-08-07）：`data/README.md` judgments 42 量表说明；任务清单断点同步（AGENTS.md 全文 V2 改写可后续增量）

---

## 当前断点

- 2026-08-09：**V2 需求—代码一致性收口完成**。采集层已按 01 表显式区分患者回答、采集指令、系统读取、医护判定与待配置条目；Mini-Cog 记词指令、便秘病程开放题、MMSE/操作类医护判定均进入正确链路，旁白推进增加步骤 ID 与播放代次校验，消除重播/继续竞态跳步。系统读取建立 20 条覆盖注册表（10 条已实现、4 条需医护/设备、6 条待定义），撤回 FRAIL/NRS2002 未有表内阈值支撑的自动推断；复用注册表增加引用、标签和重复规则校验，仅保留可追溯的确定性映射。报告统一支持中医“否”级别与关注标签口径，反向题同时展示原始分/有效分；医生审核增加删除/替换理由、同类上限、编码去重和 -100 禁忌服务端校验；患者原始录音改由受会话归属、路径边界与 HTTP Range 保护的医生端接口回放。README/data 说明已切到 V2 权威源。验证：491 个单元测试、8 个 E2E、typecheck、lint、生产构建、V2 规则转换均通过。本条为现行实现口径，覆盖下方 M9.5/M10.3 历史记录中的 FRAIL、NRS2002 初筛自动推定描述。仍需外部输入：机构预设套餐定义、数字人 SDK 商务开通、48 项干预素材、MMSE/便秘缺失标签判定规则，以及基础数据导入格式与合并策略。

- 2026-08-08：零散 UI 调整——①首页「已解锁到第 N 步」提示徽章删除（`src/app/page.tsx`，解锁逻辑保留）；②建档页新增「已经建档过？选择已有档案」区（`/patient/register` 头部下方，最近 12 位患者大按钮卡片，点选直达第二步量表选择、档案信息一并带上；demo 无鉴权仅列姓名/性别/年龄）；③**适老化简化**（用户拍板：风格简单、字体大、内容简洁）——`globals.css` 患者端 `.patient-main` 内 text-xs/sm/base/lg/xl 整体上提一档（12/14/16/18/20 → 15/17/19/21/24px），display-title/copy、patient-input 同步加大；建档/方式选择/四方式页/问询 intro 的长文案统一精简（ assists 提示、用药格式 hint、上传说明、空态文案等），医学字段与交互口径不变。485 单测绿、typecheck/lint 绿。

- 2026-08-08：**病历智能评估支持上传文件/图片**（全本机处理、不出网，PII 红线不变——提取文本进病历框后仍走既有脱敏解析链路）。产物：①`src/lib/assessment/emr-file-extract.ts`（docx 复用 scripts/read-docx 零依赖 ZIP 解析改 Buffer 输入；xlsx 用既有 xlsx 依赖）+ `POST /api/patient/emr-extract`（docx/xlsx/xls，≤30MB，截断 8000 字）；②`emr-form.tsx` 上传入口：txt/md/csv 前端直读、docx/xlsx 走服务端、PDF 前端 pdfjs-dist **v3 legacy 构建**（v6 的 Iterator helpers / async iterable ReadableStream 在演示机旧 Edge 内核不可用，v6 试装后降级）、图片前端 tesseract.js 中文 OCR（chi_sim）；③本地化运行时资产 `public/vendor/ocr/`（worker.min.js + tesseract-core 全部 4 变体 + chi_sim.traineddata）与 `public/vendor/pdf/pdf.worker.min.js`——全部本地，现场无网可用，pack-share 拷 public/ 自动包含；④`next.config.ts` turbopack.resolveAlias `canvas` → `src/lib/stubs/empty.ts`（pdfjs v3 legacy 的 Node 专用 require 兜底）；⑤`eslint.config.mjs` 补 `public/vendor/**` ignore。新依赖：tesseract.js@7、pdfjs-dist@3.11.174。chi_sim.traineddata 下载自 raw.githubusercontent（jsdelivr 无 .gz 版，用未压缩版 + `gzip:false`）。验证：485 单测绿（新增 emr-file-extract 3 用例）、typecheck/lint 绿、浏览器冒烟三条链路全通（图片 OCR 主体字段正确、11MB 含图 docx 提取截断 2000 字、PDF 逐字精确）+ 智能解析整链出推荐。已知限制：tesseract fast 模型对药名等形近字有误识（苯→茶、胍→肌），界面已提示"可继续编辑"；如需更高精度后续可换 tessdata_best。

- 2026-08-08：**患者流程每页六步进度条 + 问询页只展示当前题**（用户拍板设计口径）。新增 `src/components/patient-flow-progress.tsx`（`V2DemoPipeline` 全标题无描述无标题区模式，`hideDescriptions` 新 prop；步骤 5/6 标题改「干预匹配与决策」「干预内容展示」对齐设计图），嵌入：建档 step1、方式选择+四方式页 step2、问询 step3（`interview-screen.tsx`，舞台高度 calc 112px→220px 适配）、报告 step6（`patient-report.tsx`）。问询页移除聊天记录累积（`ConversationLog`/`talks`/`describeAnswer`/滚动 effect 全删），主区改 `CurrentPrompt` 单条大字气泡（当前题/旁白，带播报呼吸点）；作答反馈改由点选态+进入下一题承载（V2.0 §2.2 不再弹提示口径不变，注释已同步）。验证：typecheck/lint/482 单测绿、**全量 e2e 8 用例绿（4.5m）**、1080p 冒烟布局正常。

- 2026-08-08：**患者端建档页扩展为「基础信息填写」全字段三段式**（用户拍板：应用到患者端、保持仅姓名/性别/年龄三项必填）。`/patient/register` 重写：一、基本情况（+文化程度/婚姻/居住/照护下拉）；二、疾病与用药（现有诊断/既往病史/近期急性疾病/当前用药 textarea，类别限西药/中成药/保健品）；三、人体测量与客观指标（新客户端组件 `measurement-fields.tsx`：身高/体重现在+BMI 实时计算、体重史 5 时点、双腿小腿围、握力[传感器暂不接入提示]、6 米步行、DXA/BIA 占位）。`registerPatient` 接入 `buildV2ProfileExtensions`（与医生端 createPatient 同一校验、同一 Json 落库写法），非法整体回 `error=profile`。全部选填结构化落库，医生端患者详情「补充档案」区自动可见、系统读取题直接复用。验证：typecheck/lint 绿、482 单测绿、Playwright 冒烟（填 8 个选填字段提交 → 跳方式选择页，DB 核实 diagnoses/medications/weightHistory/education/calfLeftCm 结构化正确）。

- 2026-08-08：**答完自动出报告（免点击）完成**。`interview-screen.tsx`：finished 阶段（报告已随 `tryAutoFinalize` 落库）在结束语 TTS 播完后 1.5s 自动 `router.refresh()` 切到报告视图（无播报/降级路径同样自动跳，`reportJumpedRef` 去重）；「查看我的评估报告」按钮保留为兜底，完成页加"正在为您打开…"提示。e2e 配套：4 个规格（patient-flow/patient-self-register/supplementary/patient-minicog-constipation）去掉点击按钮、改为等待报告标题自动出现（30s）。**顺带修复前一会话遗留**：两步建档改版（register 只收必填+测量、量表选择拆到 /patient/select-scales）后 e2e 未同步——3 个规格（self-register/supplementary/minicog-constipation）+ voice-auto 改为「建档 → 方式选择 → 自选组合·临时自定义勾选（原默认 fall_3q+frail 两项）→ 确认并进入采集」新链路；必填校验用例按钮名改「下一步：选择评估内容」。`eslint.config.mjs` 补 `playwright-report/**`、`test-results/**` ignore（失败用例 trace 查看器打包 JS 此前被误扫，184 错）。验证：typecheck/lint 绿、482 单测绿、**全量 e2e 8 用例绿（4.8m）**。注意：Next 16 单项目只允许一个 dev server——跑 e2e 前须先停 3000 的开发服务（e2e 自带 3100 webServer + e2e.db）。

- 2026-08-08：**患者端「量表工具选择」四方式页按设计图改版完成**（用户拍板：应用于患者端、按图落地新套餐、机构预设套餐占位禁用）。产物：`src/lib/assessment/patient-scale-packages.ts`（常规综合 3 包 standard 12 项/outpatient 7 项/inpatient 9 项 + 自选预设 4 包 cognition_mood 7/fall_risk 6/sarcopenia_nutrition 8/sleep_pain 6，模块加载断言项数与可评分；`groupScalesByCategory`/`estimateMinutes`/`askableQuestionCount`/`minutesFromAskable`）；`/patient/select-scales/routine` 重写（套餐单选 + 一级分类分组配置 + 已选计数）、`emr`（病历/诊断输入 → `/api/patient/emr-suggest` → 风险关键词 chips + 推荐重点预选 + 可补充 `EMR_SUPPLEMENT_POOL` + 系统判断说明；`EmrSuggestResult` 新增 `keywords`，DeepSeek 提示词同步扩展、缺失回落规则关键词）、`followup`（既往记录 + 上次标签/干预摘要 + 重点复评默认全选 + 按需补充 + 上次/本次项目与预计时间统计条；无已出报告会话时空态引导）、`custom`（系统预设套餐/机构预设套餐[禁用占位]/临时自定义三 tab + 已选列表按来源分组可移除/清空 + 已选项数与预计分钟）；`startAssessment` 失败按 mode 回跳对应方式页。**推定待甲方确认**：门诊简版/住院入院/跌倒风险/睡眠与疼痛 4 个套餐的具体量表清单（图中未枚举，代码注释已标注）。**口径说明**：患者端随访复评既有量表为设计图指定入口（走 startAssessment 新建会话，报告页 scaleScopes 自动标「复评」出对比），与 V2.0 §3「复评属医生授权」的报告页补充评估 guard 并行不悖。医生端 SCALE_PACKAGES/session-create-form 未动。测试：`tests/patient-scale-packages.test.ts` 8 用例 + emr keywords 用例；全量 482 用例绿、typecheck/lint 绿、四方式页 dev 冒烟 200 + Playwright 走查（常规版面/随访完整态与空态/自选套餐整包选择与移除/EMR DeepSeek 实调返回关键词）。

- 2026-08-08：**五路审计 + 全量修复轮完成**。审计发现并已修复：①按钮/图片作答 score 匹配碰撞（便秘恒假阳性、Bristol 恒 1 型、居家「不适用」误记、IADL 追溯失真）→ 改 label 精确匹配（`matchButtonOption`，e2e 新增 `patient-minicog-constipation.spec.ts` 锁定）；②画钟 pending 复问轮卡死 → 跳过 drawing；③纯测量量表全豁免伪造阴性 → `guardFullyDeferred` 零标签 + 报告页「需医护测量后评定」；④平和质负向题未反向计分 → `reverseItemIds` 配置化反向（国标 CCMQ 口径，负责人已拍板）；⑤医生同类替换可绕 -100 禁忌 → UI 禁用 + `confirmPlan` 硬拦截；⑥补充评估越权 → cookie 归属 + 源会话状态校验；⑦数字题规则兜底 off-by-one、播放竞态悬挂、emr reason 死分支、PII 值级姓名替换脱敏接线；⑧system-read 补 glim_1/2/3（nrs2002_终筛1 确认推不出，注释说明）；⑨干预素材 12 项 V1→V2 映射就位 + mediaSrc 内容哈希修复；⑩随访对比展示（`scaleComparisons` 派生，报告页对比区）。验证：typecheck/convert-rules-v2 绿、473 单测绿、e2e 8 用例绿。仍开放：机构预设套餐（需甲方定义）、数字人 SDK（商务）、MMSE_UNABLE_TO_COMPLETE 无产出路径（需产品拍板）、48 项干预素材待业务方补齐。

- 2026-08-07：**收尾增强完成**：①前端六步主流程 IA（`v2-pipeline` + 医患页对齐 Demo_v2）；②复用规则扩充（运动初筛→FRAIL、跌倒→Morse、尿失禁→ICIQ）；③医患报告/审核强制干预醒目标注 + 医生 ResultView 按量表分组；④AGENTS.md 关键口径改写为 V2。全量测试绿。可选：e2e 新题型用例、数字医生 SDK、AGENTS 历史段落继续精简。
- 2026-08-07：**V2 迭代主清单项已收口**（M9.6～M9.8 / M10.2 / M11.2 / M11.3 / M11.4）。全量 394 用例绿、typecheck/build 绿、便携包 151MB 已生成。可选后续：AGENTS.md 全文按 V2 重写、e2e 对新题型补用例、数字医生 SDK。
- 2026-08-07：**M10.3b-2 完成**（可评分量表 25→42 全量；新判定类型 ladderScore/anyBelowThreshold/initialGateSumRange/compositeAllAny；产物与取舍详见清单 M10.3 条目）。全量 388 用例绿、typecheck/convert-rules-v2 绿。下一步优先 **M9.6 新题型 UI**（数字/多选/Bristol 图/画钟）或 **M9.7 医护题代填扩展** / **M11.2 报告页 190 标签展示**。
- 2026-08-07：**M10.3b 第一批完成**（判定引擎多判定器/perQuestionTags 扩展 + 老年综合征问答/社会与环境 12 量表判定，可评分量表 13→25；产物与取舍详见清单 M10.3 条目——iciq 降级口径、pain_nrs 前导分值解析、constipation_symptom Q1–Q3 不纳入等）。全量 367 用例绿、typecheck/lint/build/convert-rules-v2 绿；e2e 规格中量表数量断言已同步（13→25、11→23）。下一步 **M10.3b-2 剩余量表（阶梯/组合/测量类）**（vision/visual_function/hearing/whisper/motor_screen/sppb/home_env/braden/cam/glim/nrs2002/pain_behavior/polypharmacy/calf/grip/gait_speed/dxa_bia，需新判定类型：阶梯计分/N-A 分母/组合判定/测量阈值）或 **M9.6 新题型 UI**（数字输入落地后补 iciq Q3/Q4 多选与 constipation_symptom Q3 的 CONSTIPATION_LOW_FREQUENCY）。
- 2026-08-07：**M11.1 完成**：e2e 已按 V2 重写全绿（7 用例，详见清单 M11.1 条目）；下一步 **M10.3b 剩余量表判定**（M10.3 剩余的老年综合征/社会与环境等量表）或 **M9.6 新题型 UI**。
- 2026-08-07：**M9.3+M9.4 完成**（跨量表变量复用回填 + 分支跳过；落地范围为焦虑两问 ↔ GAD-7 条件复用一组，其余复用/分支依赖未配判定量表随 M10.3b 落地，抑郁两问→GDS-15 因 01 表未明说未门控——详见清单条目。产物：`src/lib/assessment/reuse.ts`、`src/lib/assessment/system-answers.ts`、service/finalize 两处接线、`tests/reuse.test.ts` 12 用例，全量 280 用例绿、typecheck/lint/build 通过）。下一步 **M9.6 新题型 UI**（数字输入/多选/图片选择/画钟画布）。
- 2026-08-07：**M10.1 完成**（产物：`src/lib/assessment/scale-packages.ts`、`src/app/doctor/patients/[id]/session-create-form.tsx`、`createSession` 统一解析、`tests/scale-packages.test.ts` 14 用例，全量 268 用例绿、typecheck/lint/build 通过；病历智能评估属 M10.2 未做，界面占位禁用）。下一步 **M9.3 变量复用 + M9.4 分支逻辑**。
- 2026-08-07：**M10.3a 完成**（产物：`src/lib/scoring-v2/threshold-by-education.ts` + `education.ts`（四档文化程度映射，引擎与转换脚本共用）、`data/judgments-v2.json` 补 mmse（thresholdByEducation）/gds15/gad7/ais（sumRange）、convert-rules-v2 校验段 MVP 9→13 + thresholdByEducation 校验、finalize 透传 patient.education 并透出 blockedReason（医生端 incomplete 提示附原因）、register 页补 4 量表适老化文案；`tests/scoring-v2-mmse.test.ts` 16 用例 + sum-range 增补 gds15/gad7/ais 边界，全量 254 用例绿、typecheck/lint/convert-rules-v2 绿）。取舍：MMSE 缺文化程度一律阻断（含 deferClinical 患者路径），blockedReason 非条目缺失、医生补档案后可评；MMSE_UNABLE_TO_COMPLETE 不自动判定；AIS「可能有」按 02 表建议程序判定取 4–5。下一步 **M10.1 量表工具选择界面**。
- 2026-08-07：**转换器选项解析修复**（convert-rules-v2.ts parseOptions 改「选项起点前瞻」切分 + 段末过渡语剥离）：mnasf_6 的「D、BMI≥23（3分）。无法获得BMI时按小腿围：E、＜31 cm（0分）」此前被错误并档，现正确解析为 6 个选项；system-read.ts 的临时绕行已移除（小腿围 <31cm 命中真正的「＜31 cm（0分）」选项）。重跑 convert-rules-v2 绿、全量 213 用例绿、typecheck/lint 绿。
- 2026-08-07：**M9.5 完成**（系统读取条目按档案自动作答落库，见上方清单条目）；剩余：M9.2-9.4 采集编排/复用/分支、M9.6-9.8 新题型 UI、tests/e2e 待按 V2 重写；下一步 **M9.2 采集编排**（开场白/分类过渡/工具说明播报）。
- 2026-08-07：**M9-B 装机完成**（主链路 V2：对话→评分→推荐→审核→报告）；剩余：M9.2-9.5 采集编排/复用/分支/系统读取、M9.6-9.8 新题型 UI、tests/e2e 待按 V2 重写；下一步 **M9.2 采集编排**（开场白/分类过渡播报）。
- 2026-08-06：**M9.1 完成**（产物：Patient 模型新增 13 个可空列（education/maritalStatus/livingSituation/careSituation + 五个 Json（diagnoses/pastHistory/recentAcute/medications/weightHistory）+ 四个标量（calfLeftCm/calfRightCm/gripStrengthKg/gaitSpeed6mSec）），迁移 `prisma/migrations/20260806130939_v2_patient_intake`；`src/lib/assessment/patient-intake.ts` 新增 `buildV2ProfileExtensions`（全选填，枚举/数值范围/诊断分隔/用药"药名,类别,剂量,频次"行解析，任一非法整体返 null）+ 共享枚举常量；`src/lib/actions/doctor.ts` createPatient 透传（失败回 `?error=profile`）；医生端新建患者页加「基本情况补充/疾病与用药/测量补充」三分区，患者详情页新增"补充档案（V2）"展示区（空不显示、6 米用时换算 m/s）；患者自助建档简版未动；`tests/patient-intake.test.ts` 新增 8 用例，全量回归 292 用例全绿、typecheck/lint/build 通过、dev 冒烟 200）。下一步 **M9-B 运行时装机**（dialogue/finalize/报告页接 V2 数据，M8 推迟项）。
- 2026-08-06：**M8 完成**（产物：`src/lib/recommend-v2/`（纯函数引擎 `recommendV2(tagCodes)`：未知编码抛错、placeholder 静默剔除、-100 一票禁止（安全优先于 100）、100 forced 置顶不占每类 2 名额、2–10 累加 total（forced 项 total 约定不含 100 标记）、每类 total 降序/同分编码升序取前 2、输出 categories + forced 平铺 + forbidden 禁止原因明细）、`src/components/intervention-media.tsx` 新增 `InterventionText` 文本卡（纯增量导出）、`tests/recommend-v2.test.ts` 13 用例全绿、全量回归 284 用例全绿、typecheck/lint 通过；报告页"先结论后干预"核查现状已满足未改代码；运行时装机（dialogue/finalize/报告页接 V2 数据、CATEGORY_ORDER 5 类、JZ01 醒目标注、素材待补齐展示）统一推迟 M9）。下一步 **M9.1** Patient 模型扩展。
- 2026-08-06：**M7 完成**（产物：`data/judgments-v2.json`（MVP 8 量表判定配置，手工整理 + convert-rules-v2 校验段）、`src/lib/rules/v2.ts`（V2 规则唯一类型化读取入口）、`src/lib/scoring-v2/`（纯函数引擎：scoreScaleV2/scoreAllV2，sumRange/anyYes/tcmConstitutionV2 三判定器，deferClinical 口径沿用旧引擎）、`tests/scoring-v2-*.test.ts` 80 用例全绿、全量回归 271 用例全绿、typecheck/lint/convert-rules-v2 均通过；perQuestionTags/thresholdByEducation/composite 推迟 M10）。下一步 **M8.1** 推荐引擎重写（编码索引、100 置顶/-100 剔除/2–10 累加、5 大类各取前 2）。
- 2026-08-06：M6 完成（产物：`scripts/convert-rules-v2.ts` + `npm run convert-rules-v2`；`data/scales-v2.json`（42 量表/238 条目/20 旁白）、`data/result-tags.json`（190 标签/5 占位）、`data/intervention-scoring-v2.json`（稀疏矩阵非零 638 条，黄金向量生成期自校验通过）、`data/interventions-v2.json`（60 项）、`public/interventions/bristol-stool.{png,webp}`；`tests/convert-rules-v2.test.ts` 12 用例全绿）。下一步 **M7.1** judgment 类型扩展。
- 调研产物（会话临时，可重生成）：`tmp/v2-doc.txt`（docx 全文 32 段）、`tmp/v2-01..04_*.txt`（4 张 xlsx 转出文本，重生成命令：`npx tsx tmp/dump-v2-xlsx.ts`）。
- 已提取的推荐黄金向量（供 M6.7 测试）：FRAIL_FRAIL→YD01:6/YD02:9/SS02:9/SS03:8/ZY01:6/JZ02:8/JZ03:7/QT15:6；MNA_SF_MALNUTRITION_RISK→SS02:7/SS03:8/SS04:7/ZY01:5/JZ02:5/JZ08:8；GLIM_SEVERE_MALNUTRITION→SS02:9/SS03:10/SS04:9/ZY01:5/JZ02:5/JZ08:10；MORSE_FALL_RISK_HIGH→YD02:8/YD04:7/YD06:9/YD07:-100/JZ03:9/QT01:7/QT02:7/QT03:7/QT04:7/QT05:9；ADL_DEPENDENCE_SEVERE→YD02:9/YD03:7/YD07:-100/YD10:7/JZ02:7/JZ03:8/QT05:7/QT15:7；SARCOPENIA_DIAGNOSED→YD02:10/YD03:8/YD04:6/YD08:6/SS02:10/SS03:8/ZY01:5/JZ03:9/JZ08:7；TCM_QI_DEFICIENCY_YES→YD01:4/SS02:4/ZY01:9；HOME_FLOOR_SLIPPERY→QT02:10/QT05:3；POLYPHARMACY_PRESENT→JZ02:5/JZ14:9/QT10:8；CAM_DELIRIUM_POSITIVE→JZ01:100/JZ02:8/QT12:5/QT15:8 + 其余 30 项 YD/SS/ZY 全 -100。
